_build/
rebar3.crashdump
erl_crash.dump
*.beam

# C build artifacts under _checkouts/erl_ts (and anywhere else) — always
# regenerated by `make`/`rebar3 compile`, never meant to be committed.
*.o
*.a
*.so
*.dylib
tree-sitter.pc
