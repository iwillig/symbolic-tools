-module(sample).
-export([foo/1, other/0, double/1]).

foo(X) ->
    bar(X),
    baz:qux(X, 1).

other() ->
    bar(ok).

%% Doubles a number.
double(X) ->
    X * 2.

%% standalone comment, not attached to anything
