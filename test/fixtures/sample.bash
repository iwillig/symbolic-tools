# A .bash-extension fixture (sample.sh already covers ".sh") — exists
# only to exercise the ".bash" branch of
# symbolic_codebase:language_from_ext/1, which nothing else under
# test/fixtures/ triggers.
greet() {
  echo hi
}
